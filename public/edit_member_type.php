<?php require_once("../includes/session.php"); ?><?php require_once("../includes/db_connection.php"); ?><?php require_once("../includes/functions.php"); ?><?php require_once("../includes/validation_functions.php"); ?><?php     // use the function ConfirmLoggedIn to see if the current user        // is allowed to view this page    ConfirmLoggedIn();?><?php    $User = FindMemberById($_SESSION["MemberId"]);        // Check to see if the current user can edit members.    // If not, then the user cannot have access to this page.    if($User["EditMembers"] == 0) {        RedirectTo("index.php");    }?><?php  $MemberType = FindMemberTypeById($_GET["id"]);  if (!$MemberType) {    // admin ID was missing or invalid or     // admin couldn't be found in database    redirect_to("manage_member_types.php");  }?><?phpif (isset($_POST['submit'])) {  // Process the form  // validations  $required_fields = array("MemberTypeName");  ValidatePresences($required_fields);  $fields_with_max_lengths = array("MemberTypeName" => 20);  ValidateMaxLengths($fields_with_max_lengths);  if (empty($errors)) {    // Perform Update    $id = $MemberType["MemberTypeId"];    $MemberTypeName = MysqlPrep($_POST["MemberTypeName"]);    $query  = "UPDATE MemberTypes SET ";    $query .= "MemberTypeName = '{$MemberTypeName}' ";    $query .= "WHERE MemberTypeId = {$id} ";    $query .= "LIMIT 1";    $result = mysqli_query($connection, $query);    if ($result == 1) {      // Success      $_SESSION["message"] = "Member Type updated.";      RedirectTo("manage_member_types.php");    } else {      // Failure      $_SESSION["message"] = "Member Type update failed.";    }  }} else {  // This is probably a GET request} // end: if (isset($_POST['submit']))?><?php $layout_context = "admin"; ?><?php include("../includes/layouts/header.php"); ?><div id="main">  <div id="navigation">    &nbsp;  </div>  <div id="page">    <?php echo message(); ?>    <?php echo FormErrors($errors); ?>    <h2>Edit Member Type: <?php echo htmlentities($MemberType["MemberTypeName"]); ?></h2>    <form action="edit_member_type.php?id=<?php echo urlencode($MemberType["MemberTypeId"]); ?>" method="post">      <p>Type:        <input type="text" name="MemberTypeName" value="<?php echo htmlentities($MemberType["MemberTypeName"]); ?>" />      </p>      <input type="submit" name="submit" value="Save Changes" />    </form>    <br />    <a href="manage_member_types.php">Cancel</a>  </div></div><?php include("../includes/layouts/footer.php"); ?>